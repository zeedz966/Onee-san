const { GuildMember, User } = require("discord.js")

module.exports = {
    name: 'adminbaper?',
    description: "kukuh cikal!",
    execute(message, args){
        message.channel.send(`https://www.facebook.com/kukuh.cikal`)
    }
}